#! /bin/bash

if command -v rpm-ostree > /dev/null; then
	if [ "$1" == "check" ]; then
		rpm-ostree upgrade
	elif [ "$1" == "--supports-duplicate-detection" ]; then
		exit 0
	else
		rpm-ostree upgrade
	fi
else
    exit 7
fi
